Отрицательная цена:

INSERT INTO `goods` 
    (`name`, `vendor_code`, `price`)
    VALUES
    ('Точилка для карандашей', '54445454', -145);

/* Запрос выполнен, отрицательная цена вставлена */


Количество товара на складе < 0:

INSERT INTO `goods` 
    (`name`, `vendor_code`, `amount`)
    VALUES
    ('Точилка для карандашей', '545454', -1);

/* Запрос выполнен успешно, в поле `amount` установлено значение 0 
(нижний предел указанного при создании таблицы диапазона - INT UNSIGNED) */


Пустой артикул:

INSERT INTO `goods` 
    (`name`, `vendor_code`)
    VALUES
    ('Точилка для карандашей', '');

INSERT INTO `goods` 
    (`name`)
    VALUES
    ('Точилка для карандашей');

/* Любой один из двух запросов будет выполнен успешно, в поле артикула будет вставлена пустая строка.
 Однако, повторная попытка вставить товар с пустым артикулом приводит к ошибке:
#1062 - Дублирующаяся запись '' по ключу 'vendor_code' */


Попытка вставить второй товар с тем же артикулом:

INSERT INTO `goods` 
    (`name`, `vendor_code`, `amount`)
    VALUES
    ('Точилка для карандашей', '54445454', -1);

/* Ошибка: #1062 - Дублирующаяся запись '54445454' по ключу 'vendor_code' */


Пустое наменование товара:

INSERT INTO `goods` 
    (`vendor_code`)
    VALUES
    (56565);

/* Запрос выполнен успешно, вставлена запись с пустым  наименованием. 
Все последующие запросы с пустым именем товара также выполняются */


Вставка записи с собственным первичным ключом:

INSERT INTO `goods` 
    (`id`, `vendor_code`)
    VALUES
    (56, 'js4545');

/* Запрос выполнен, данные вставлены. 
Все последующие товары получат при вставке первичные ключи, 
следующие по порядку за уставновленным вручную (57, 58 и т.д.) */


Вставка неверного формата даты:

INSERT INTO `goods` 
    (`vendor_code`, `sale_from`)
    VALUES
    (565465, 'dsfsfasas');

/* Запрос выполняется, в поле с типом DATE установлено значение '0000-00-00' */


